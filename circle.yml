machine:
  node:
    version: 8.1.0

dependencies:
  override:
    - npm install
    - pip install -r requirements_test.txt

test:
  override:
    - make test

deployment:
  build_package:
    branch: master
    commands:
      - make publish
